<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aarthi.anti_corruption" >

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!--
         The following two permissions are not required to use
         Google Maps Android API v2, but are recommended.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="corruption spotted!"
        android:theme="@style/AppTheme" >

        <activity android:name=".homescreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Newuser"></activity>
        <activity android:name=".Passcheck"></activity>
        <activity android:name=".mainscreen"></activity>
        <activity android:name=".complaint"></activity>
        <activity android:name=".GroundOverlayDemoActivity" >
        </activity>
        <activity android:name=".forumactivity"></activity>
        <activity android:name=".CameraPhotoCapture" >
        </activity>
        <activity android:name=".repmap"></activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyAJQu0qKM3NLbB4OLkHTC54i1cI1wTkhZo" />

    </application>

</manifest>
